# 腾讯云 CloudBase 配置
# 在 CloudBase 控制台 → 环境 → 环境总览 获取环境 ID
VITE_CLOUDBASE_ENV_ID=
# 地域，默认上海（ap-shanghai）
VITE_CLOUDBASE_REGION=ap-shanghai

# 设为 true 时使用本地演示模式（无需 CloudBase，方便预览 UI）
VITE_DEMO_MODE=true
